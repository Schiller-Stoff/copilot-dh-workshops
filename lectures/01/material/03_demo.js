


/**
 * Calculates the mean age of given numbers
* @param {number[]} numbers - The numbers to calculate the average of  
* @returns {number} The average of the given numbers
 */
const calculateAverage = (numbers) => {
    // trigger autocomplete via "ALT + ^" 
    // switch suggestions via "ALT + `" and "ALT + ß"
    // or: use "generate this"
    // remove commends if copilot is being confused




};